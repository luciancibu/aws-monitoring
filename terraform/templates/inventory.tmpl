all:
  vars:
    ansible_ssh_private_key_file: ${clientkey}.pem
    flask_port: 5000
    
  hosts:
    app:
      target_private_ip: ${python_ip}
      target_public_ip: ${python_ip_public}
      target_ansible_user: ${python_user}
      



